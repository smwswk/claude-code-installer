# 不可压缩锚点规则

以下规则在上下文压缩时保持高权重，任何推理不得违反。

## 记忆协议
- 会话启动必须读 MEMORY.md，优先关注「活跃工作区」section
- 需要查询稳定参考/方法论时，读 REFERENCE_INDEX.md 定位后读取具体文件
- 会话启动检查清单：(1) 读 CURRENT_STATE.json 了解活跃任务 (2) 查 MEMORY.md 有无 [待审核] 标记 (3) 待审核项优先处理

## 输出规则
- 输出路径在 ~/.claude/ 或 /tmp/ 下时，使用系统文件管理器揭示
- 展示 md 内容用 markdown 阅读器打开，不要在终端 cat
- 回复要简洁直接，不自言自语

## 安全规则
- 删除操作：先列清单获授权 → 确认 symlink 真实路径 → 待删文件放 /_to_delete_待确认/ 最终确认
- 不操作活跃应用缓存（~/Library/Caches/*），除非先停应用
- 不发明 URL，不确定时直接问
